name: Release Build
on:
  workflow_dispatch:
    inputs:
      version:
        description: The version to tag the release with, e.g., 1.2.0
        required: true
      aws_region:
        description: 'Deploy lambda layer to aws regions'
        required: true
        default: 'us-east-1, us-east-2, us-west-1, us-west-2, ap-south-1, ap-northeast-3, ap-northeast-2, ap-southeast-1, ap-southeast-2, ap-northeast-1, ca-central-1, eu-central-1, eu-west-1, eu-west-2, eu-west-3, eu-north-1, sa-east-1, af-south-1, ap-east-1, ap-south-2, ap-southeast-3, ap-southeast-4, eu-central-2, eu-south-1, eu-south-2, il-central-1, me-central-1, ap-southeast-5, ap-southeast-7, mx-central-1, ca-west-1, cn-north-1, cn-northwest-1, ap-east-2, ap-southeast-6'

env:
  AWS_DEFAULT_REGION: us-east-1
  AWS_PUBLIC_ECR_REGION: us-east-1
  AWS_PRIVATE_ECR_REGION: us-west-2
  RELEASE_PUBLIC_REPOSITORY: public.ecr.aws/aws-observability/adot-autoinstrumentation-python
  RELEASE_PRIVATE_REPOSITORY: 020628701572.dkr.ecr.us-west-2.amazonaws.com/adot-autoinstrumentation-python
  RELEASE_PRIVATE_REGISTRY: 020628701572.dkr.ecr.us-west-2.amazonaws.com
  PACKAGE_NAME: aws-opentelemetry-distro
  VERSION: ${{ github.event.inputs.version }}
  AWS_REGIONS: ${{ github.event.inputs.aws_region }}
  WHEEL_ARTIFACT_NAME: aws_opentelemetry_distro-${{ github.event.inputs.version }}-py3-none-any.whl
  SOURCE_ARTIFACT_NAME: aws_opentelemetry_distro-${{ github.event.inputs.version }}.tar.gz
  # Legacy list of commercial regions to deploy to. New regions should NOT be added here, and instead should be added to the `aws_region` default input to the workflow.
  LEGACY_COMMERCIAL_REGIONS: us-east-1, us-east-2, us-west-1, us-west-2, ap-south-1, ap-northeast-3, ap-northeast-2, ap-southeast-1, ap-southeast-2, ap-northeast-1, ca-central-1, eu-central-1, eu-west-1, eu-west-2, eu-west-3, eu-north-1, sa-east-1
  LAYER_NAME: AWSOpenTelemetryDistroPython
  LAYER_ARTIFACT_NAME: aws-opentelemetry-python-layer.zip
  TEST_TAG: adot-autoinstrumentation-python:test

permissions:
  id-token: write
  contents: write

jobs:
  download-artifacts:
    environment: Release
    runs-on: ubuntu-latest
    outputs:
      aws_regions_json: ${{ steps.set-matrix.outputs.aws_regions_json }}
    steps:
      - name: Checkout Repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0

      - name: Get successful main build run
        id: main_build
        uses: ./.github/actions/get_main_build_run
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.ref_name }}

      - name: Download main build artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_ID: ${{ steps.main_build.outputs.run_id }}
        run: |
          gh run download "$RUN_ID" -n release-artifacts -D dist
          gh run download "$RUN_ID" -n layer.zip -D layer

      - name: Upload SDK wheel artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4.6.2
        with:
          name: ${{ env.WHEEL_ARTIFACT_NAME }}
          path: dist/${{ env.WHEEL_ARTIFACT_NAME }}

      - name: Upload SDK source artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4.6.2
        with:
          name: ${{ env.SOURCE_ARTIFACT_NAME }}
          path: dist/${{ env.SOURCE_ARTIFACT_NAME }}

      - name: Upload layer artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4.6.2
        with:
          name: layer.zip
          path: layer/${{ env.LAYER_ARTIFACT_NAME }}

      - name: Set up regions matrix
        id: set-matrix
        run: |
          IFS=',' read -ra REGIONS <<< "${{ env.AWS_REGIONS }}"
          MATRIX="["
          for region in "${REGIONS[@]}"; do
            trimmed_region=$(echo "$region" | xargs)
            MATRIX+="\"$trimmed_region\","
          done
          MATRIX="${MATRIX%,}]"
          echo ${MATRIX}
          echo "aws_regions_json=${MATRIX}" >> $GITHUB_OUTPUT

  publish-sdk:
    needs: download-artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@e468171a9de216ec08956ac3ada2f0791b6bd435 #3.11.1

      - name: Configure AWS credentials for private ECR
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 #v5.0.0
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_ECR_RELEASE }}
          aws-region: ${{ env.AWS_PRIVATE_ECR_REGION }}

      - name: Log in to AWS private ECR
        uses: docker/login-action@184bdaa0721073962dff0199f1fb9940f07167d1 #v3.5.0
        with:
          registry: ${{ env.RELEASE_PRIVATE_REGISTRY }}

      - name: Configure AWS credentials for public ECR
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 #v5.0.0
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_ECR_RELEASE }}
          aws-region: ${{ env.AWS_PUBLIC_ECR_REGION }}

      - name: Log in to AWS public ECR
        uses: docker/login-action@184bdaa0721073962dff0199f1fb9940f07167d1 #v3.5.0
        with:
          registry: public.ecr.aws
     
      - name: Download SDK wheel artifact
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          name: ${{ env.WHEEL_ARTIFACT_NAME }}
          path: dist-pypi

      - name: Download SDK source artifact
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          name: ${{ env.SOURCE_ARTIFACT_NAME }}
          path: dist-pypi

      # The step below publishes to TestPyPI in order to catch any issues
      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e #v1.13.0
        with:
          repository-url: https://test.pypi.org/legacy/
          attestations: false
          skip-existing: true
          verbose: true
          packages-dir: dist-pypi

      # Publish to prod PyPI
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e #v1.13.0
        with:
          skip-existing: true
          verbose: true
          packages-dir: dist-pypi

      # QEMU lets this amd64 runner build and run the arm64 image so both released
      # architectures are verified before publishing.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@29109295f81e9208d7d86ff1c6c12d2833863392 #3.6.0

      # Pre-publish gate: build each released architecture locally (not pushed) and verify it, so
      # a failure here stops the job before the push.
      - name: Build amd64 image for testing
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 #v6.18.0
        with:
          push: false
          load: true
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          tags: ${{ env.TEST_TAG }}-amd64

      - name: Verify amd64 image
        shell: bash
        env:
          DOCKER_DEFAULT_PLATFORM: linux/amd64
        run: .github/scripts/test-adot-python-image.sh "${{ env.TEST_TAG }}-amd64" "${{ env.VERSION }}" "dist-pypi/${{ env.WHEEL_ARTIFACT_NAME }}"

      - name: Build arm64 image for testing
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 #v6.18.0
        with:
          push: false
          load: true
          context: .
          file: ./Dockerfile
          platforms: linux/arm64
          tags: ${{ env.TEST_TAG }}-arm64

      - name: Verify arm64 image
        shell: bash
        env:
          DOCKER_DEFAULT_PLATFORM: linux/arm64
        run: .github/scripts/test-adot-python-image.sh "${{ env.TEST_TAG }}-arm64" "${{ env.VERSION }}" "dist-pypi/${{ env.WHEEL_ARTIFACT_NAME }}"

      - name: Build and push image
        uses: docker/build-push-action@263435318d21b8e681c14492fe198d362a7d2c83 #v6.18.0
        with:
          push: true
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.RELEASE_PUBLIC_REPOSITORY }}:v${{ env.VERSION }}
            ${{ env.RELEASE_PRIVATE_REPOSITORY }}:v${{ env.VERSION }}

      # Re-run the verification against the ACTUAL published multi-arch image (pulling each arch
      # back from the registry)
      - name: Verify published amd64 image
        shell: bash
        env:
          DOCKER_DEFAULT_PLATFORM: linux/amd64
        run: .github/scripts/test-adot-python-image.sh "${{ env.RELEASE_PUBLIC_REPOSITORY }}:v${{ env.VERSION }}" "${{ env.VERSION }}" "dist-pypi/${{ env.WHEEL_ARTIFACT_NAME }}"

      - name: Verify published arm64 image
        shell: bash
        env:
          DOCKER_DEFAULT_PLATFORM: linux/arm64
        run: .github/scripts/test-adot-python-image.sh "${{ env.RELEASE_PUBLIC_REPOSITORY }}:v${{ env.VERSION }}" "${{ env.VERSION }}" "dist-pypi/${{ env.WHEEL_ARTIFACT_NAME }}"

      - name: Tag platform images in private ECR
        env:
          REPO_NAME: adot-autoinstrumentation-python
          VERSION: ${{ env.VERSION }}
          REGION: ${{ env.AWS_PRIVATE_ECR_REGION }}
        run: |
          set -euo pipefail

          # Read the manifest index for the multi-arch tag we just pushed
          MANIFEST_INDEX=$(aws ecr batch-get-image \
            --repository-name "${REPO_NAME}" \
            --image-ids imageTag="v${VERSION}" \
            --region "${REGION}" \
            --query 'images[0].imageManifest' \
            --output text)

          # Validate the response is a non-empty manifest index
          if [ -z "$MANIFEST_INDEX" ] || [ "$MANIFEST_INDEX" = "None" ]; then
            echo "ERROR: aws ecr batch-get-image returned an empty manifest for v${VERSION}"
            exit 1
          fi
          if ! echo "$MANIFEST_INDEX" | jq -e '.manifests | type == "array" and length > 0' > /dev/null; then
            echo "ERROR: manifest for v${VERSION} is not a non-empty image index"
            echo "$MANIFEST_INDEX"
            exit 1
          fi

          # Tag platform images (amd64, arm64) with v$VERSION-$arch
          for arch in amd64 arm64; do
            DIGEST=$(echo "$MANIFEST_INDEX" | jq -r --arg a "$arch" '.manifests[] | select(.platform.architecture == $a) | .digest')
            if [ -z "$DIGEST" ] || [ "$DIGEST" = "null" ]; then
              echo "ERROR: no ${arch} image found in manifest index for v${VERSION}"
              exit 1
            fi
            MANIFEST=$(aws ecr batch-get-image \
              --repository-name "${REPO_NAME}" \
              --image-ids imageDigest="${DIGEST}" \
              --region "${REGION}" \
              --query 'images[0].imageManifest' \
              --output text)
            if [ -z "$MANIFEST" ] || [ "$MANIFEST" = "None" ]; then
              echo "ERROR: failed to fetch ${arch} platform manifest at ${DIGEST}"
              exit 1
            fi
            aws ecr put-image \
              --repository-name "${REPO_NAME}" \
              --image-tag "v${VERSION}-${arch}" \
              --image-manifest "$MANIFEST" \
              --region "${REGION}" > /dev/null
            echo "Tagged ${DIGEST} as v${VERSION}-${arch}"
          done

          # Tag attestation manifests (architecture=unknown) with their sha hex.
          # Zero attestations is a valid state for some buildkit configurations,
          # so the loop running zero times is not an error.
          for DIGEST in $(echo "$MANIFEST_INDEX" | jq -r '.manifests[] | select(.platform.architecture=="unknown") | .digest'); do
            if [ -z "$DIGEST" ] || [ "$DIGEST" = "null" ]; then
              continue
            fi
            MANIFEST=$(aws ecr batch-get-image \
              --repository-name "${REPO_NAME}" \
              --image-ids imageDigest="${DIGEST}" \
              --region "${REGION}" \
              --query 'images[0].imageManifest' \
              --output text)
            if [ -z "$MANIFEST" ] || [ "$MANIFEST" = "None" ]; then
              echo "ERROR: failed to fetch attestation manifest at ${DIGEST}"
              exit 1
            fi
            SHA_HEX="${DIGEST#sha256:}"
            aws ecr put-image \
              --repository-name "${REPO_NAME}" \
              --image-tag "${SHA_HEX}" \
              --image-manifest "$MANIFEST" \
              --region "${REGION}" > /dev/null
            echo "Tagged ${DIGEST} as ${SHA_HEX} (attestation)"
          done
  
  publish-layer-prod:
    runs-on: ubuntu-latest
    needs: [download-artifacts, publish-sdk]
    strategy:
      matrix:
        aws_region: ${{ fromJson(needs.download-artifacts.outputs.aws_regions_json) }}
    steps:
      - name: role arn
        env:
          LEGACY_COMMERCIAL_REGIONS: ${{ env.LEGACY_COMMERCIAL_REGIONS }}
        run: |
          LEGACY_COMMERCIAL_REGIONS_ARRAY=(${LEGACY_COMMERCIAL_REGIONS//,/ })
          FOUND=false
          for REGION in "${LEGACY_COMMERCIAL_REGIONS_ARRAY[@]}"; do
            if [[ "$REGION" == "${{ matrix.aws_region }}" ]]; then
              FOUND=true
              break
            fi
          done
          if [ "$FOUND" = true ]; then
            echo "Found ${{ matrix.aws_region }} in LEGACY_COMMERCIAL_REGIONS"
            SECRET_KEY="LAMBDA_LAYER_RELEASE"
          else
            echo "Not found ${{ matrix.aws_region }} in LEGACY_COMMERCIAL_REGIONS"
            SECRET_KEY="${{ matrix.aws_region }}_LAMBDA_LAYER_RELEASE"
          fi
          SECRET_KEY=${SECRET_KEY//-/_}
          echo "SECRET_KEY=${SECRET_KEY}" >> $GITHUB_ENV
      - uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 #v5.0.0
        with:
          role-to-assume: ${{ secrets[env.SECRET_KEY] }}
          role-duration-seconds: 1200
          aws-region: ${{ matrix.aws_region }}
      - name: Get s3 bucket name for release 
        run: |
          echo BUCKET_NAME=python-lambda-layer-${{ github.run_id }}-${{ matrix.aws_region }} | tee --append $GITHUB_ENV
      - name: download layer.zip
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          name: layer.zip
      
      - name: Upload to S3 and Sign
        continue-on-error: true
        run: |
          aws s3 mb s3://${{ env.BUCKET_NAME }}
          aws s3 cp ${{ env.LAYER_ARTIFACT_NAME }} s3://${{ env.BUCKET_NAME }}

          # Sign the layer
          PROFILE=$(aws signer list-signing-profiles --query "profiles[?profileName=='ADOTLambdaLayerSigningProfile'].arn" --output text 2>/dev/null)
          [ -z "$PROFILE" ] && echo "No signing profile found, skipping" && exit 0

          JOB_ID=$(aws signer start-signing-job \
            --source "s3={bucketName=${{ env.BUCKET_NAME }},key=${{ env.LAYER_ARTIFACT_NAME }},version=null}" \
            --destination "s3={bucketName=${{ env.BUCKET_NAME }},prefix=signed-}" \
            --profile-name ADOTLambdaLayerSigningProfile \
            --query 'jobId' --output text)

          aws signer wait successful-signing-job --job-id "$JOB_ID"

          SIGNED=$(aws signer describe-signing-job --job-id "$JOB_ID" --query 'signedObject.s3.key' --output text)
          aws s3 rm "s3://${{ env.BUCKET_NAME }}/${{ env.LAYER_ARTIFACT_NAME }}"
          aws s3 mv "s3://${{ env.BUCKET_NAME }}/$SIGNED" "s3://${{ env.BUCKET_NAME }}/${{ env.LAYER_ARTIFACT_NAME }}"
      
      - name: Publish Layer Version
        run: |
          layerARN=$(
            aws lambda publish-layer-version \
              --layer-name ${{ env.LAYER_NAME }} \
              --content S3Bucket=${{ env.BUCKET_NAME }},S3Key=${{ env.LAYER_ARTIFACT_NAME }} \
              --compatible-runtimes python3.10 python3.11 python3.12 python3.13 python3.14  \
              --compatible-architectures "arm64" "x86_64" \
              --license-info "Apache-2.0" \
              --description "AWS Distro of OpenTelemetry Lambda Layer for Python Runtime" \
              --query 'LayerVersionArn' \
              --output text
          )
          echo $layerARN
          echo "LAYER_ARN=${layerARN}" >> $GITHUB_ENV
          mkdir ${{ env.LAYER_NAME }}
          echo $layerARN > ${{ env.LAYER_NAME }}/${{ matrix.aws_region }}
          cat ${{ env.LAYER_NAME }}/${{ matrix.aws_region }}
          
          # Output SigningProfileVersionArn
          aws lambda get-layer-version-by-arn \
            --arn $layerARN \
            --output json | jq -r '.Content.SigningProfileVersionArn'
      - name: public layer
        run: |
          layerVersion=$(
            aws lambda list-layer-versions \
              --layer-name ${{ env.LAYER_NAME }} \
              --query 'max_by(LayerVersions, &Version).Version'
          )
          aws lambda add-layer-version-permission \
            --layer-name ${{ env.LAYER_NAME }} \
            --version-number $layerVersion \
            --principal "*" \
            --statement-id publish \
            --action lambda:GetLayerVersion
      - name: upload layer arn artifact
        if: ${{ success() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #v4.6.2
        with:
          name: ${{ env.LAYER_NAME }}-${{ matrix.aws_region }}
          path: ${{ env.LAYER_NAME }}/${{ matrix.aws_region }}
      - name: clean s3
        if: always()
        run: |
          aws s3 rb --force s3://${{ env.BUCKET_NAME }}

  generate-lambda-release-note:
    runs-on: ubuntu-latest
    needs: publish-layer-prod
    outputs:
      layer-note: ${{ steps.layer-note.outputs.layer-note }}
    steps:
      - name: Checkout Repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0
      - uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd #v3.1.2
      - name: download layerARNs
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          pattern: ${{ env.LAYER_NAME }}-*
          path: ${{ env.LAYER_NAME }}
          merge-multiple: true
      - name: show layerARNs
        run: |
          for file in ${{ env.LAYER_NAME }}/*
          do
          echo $file
          cat $file
          done
      - name: generate layer-note
        id: layer-note
        working-directory: ${{ env.LAYER_NAME }}
        run: |
          echo "| Region | Layer ARN |" >> ../layer-note
          echo "|  ----  | ----  |" >> ../layer-note
          for file in *
          do
          read arn < $file
          echo "| " $file " | " $arn " |" >> ../layer-note
          done
          cd ..
          {
            echo "layer-note<<EOF"
            cat layer-note
            echo "EOF"
          } >> $GITHUB_OUTPUT
          cat layer-note
      - name: generate tf layer
        working-directory: ${{ env.LAYER_NAME }}
        run: |
          echo "locals {" >> ../layer_arns.tf
          echo "  sdk_layer_arns = {" >> ../layer_arns.tf
          for file in *
          do
          read arn < $file
          echo "    \""$file"\" = \""$arn"\"" >> ../layer_arns.tf
          done
          cd ..
          echo "  }" >> layer_arns.tf
          echo "}" >> layer_arns.tf
          terraform fmt layer_arns.tf
          cat layer_arns.tf
      - name: generate layer ARN constants for CDK
        working-directory: ${{ env.LAYER_NAME }}
        run: |
          echo "{" > ../layer_cdk
          for file in *; do
            read arn < "$file"
            echo "    \"$file\": \"$arn\"," >> ../layer_cdk
          done
          echo "}" >> ../layer_cdk
          cat ../layer_cdk

  publish-github:
    needs: generate-lambda-release-note
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo @ SHA - ${{ github.sha }}
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 #v5.0.0
      
      - name: Download SDK wheel artifact
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          name: ${{ env.WHEEL_ARTIFACT_NAME }}
      
      - name: Download layer.zip artifact
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 #v5.0.0
        with:
          name: layer.zip

      - name: Rename layer file
        run: |
          cp ${{ env.LAYER_ARTIFACT_NAME }} layer.zip

      - name: Prepare Python release notes
        uses: ./.github/actions/prepare_python_release_notes
        with:
          pyproject-file: aws-opentelemetry-distro/pyproject.toml
          changelog-file: CHANGELOG.md
          version: ${{ env.VERSION }}

      # Publish to GitHub releases
      - name: Create GH release
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cat >> release_notes.md << EOF

          ## Release Artifacts

          This release publishes to public ECR and PyPi.
          * See ADOT Python auto-instrumentation Docker image v$VERSION in our public ECR repository:
          https://gallery.ecr.aws/aws-observability/adot-autoinstrumentation-python
          * See version $VERSION in our PyPi repository:
          https://pypi.org/project/aws-opentelemetry-distro/

          ## Lambda Layer

          This release includes the AWS OpenTelemetry Lambda Layer for Python version $VERSION-$(echo $GITHUB_SHA | cut -c1-7).

          Lambda Layer ARNs:
          ${{ needs.generate-lambda-release-note.outputs.layer-note }}
          EOF

          shasum -a 256 ${{ env.WHEEL_ARTIFACT_NAME }} > ${{ env.WHEEL_ARTIFACT_NAME }}.sha256
          shasum -a 256 layer.zip > layer.zip.sha256

          gh release create --target "$GITHUB_REF_NAME" \
             --title "Release v$VERSION" \
             --notes-file release_notes.md \
             --draft \
             "v$VERSION" \
             ${{ env.WHEEL_ARTIFACT_NAME }} \
             ${{ env.WHEEL_ARTIFACT_NAME }}.sha256 \
             layer.zip \
             layer.zip.sha256

  # Best-effort signing of the released public ECR image. Runs last, after all
  # publishing is done, and every step is continue-on-error so a signing failure
  # never fails the release run or affects already-published artifacts.
  sign-public-ecr-image:
    runs-on: ubuntu-latest
    needs: publish-github
    steps:
      - name: Configure AWS Credentials for public ECR
        continue-on-error: true
        uses: aws-actions/configure-aws-credentials@a03048d87541d1d9fcf2ecf528a4a65ba9bd7838 #v5.0.0
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_ECR_RELEASE }}
          aws-region: ${{ env.AWS_PUBLIC_ECR_REGION }}

      # Install notation CLI with AWS Signer plugin
      - name: Install notation CLI with AWS Signer plugin
        continue-on-error: true
        run: |
          curl -Lo aws-signer-notation-cli_amd64.deb https://d2hvyiie56hcat.cloudfront.net/linux/amd64/installer/deb/latest/aws-signer-notation-cli_amd64.deb
          sudo dpkg -i aws-signer-notation-cli_amd64.deb
          notation version
          notation plugin ls

      # Query ECR signing profile ARN
      - name: Query ECR Signing Profile ARN
        id: ecr-signing-profile
        continue-on-error: true
        run: |
          PROFILE_ARN=$(aws signer get-signing-profile --region ${{ env.AWS_PUBLIC_ECR_REGION }} --profile-name ADOTECRSigningProfile --query "arn" --output text) || PROFILE_ARN=""
          if [ -n "$PROFILE_ARN" ] && [ "$PROFILE_ARN" != "None" ]; then
            echo "profile_arn=$PROFILE_ARN" >> $GITHUB_OUTPUT
            echo "Found ECR signing profile: $PROFILE_ARN"
          else
            echo "ECR signing profile 'ADOTECRSigningProfile' not found or lookup failed; skipping image signing."
            exit 0
          fi

      # Login to Public ECR
      - name: Log in to AWS public ECR
        if: steps.ecr-signing-profile.outputs.profile_arn != ''
        continue-on-error: true
        uses: docker/login-action@184bdaa0721073962dff0199f1fb9940f07167d1 #v3.5.0
        with:
          registry: public.ecr.aws

      # Sign Public ECR Image
      - name: Sign Public ECR Image
        if: steps.ecr-signing-profile.outputs.profile_arn != ''
        continue-on-error: true
        run: |
          # notation needs a digest ref to store the signature via the Referrers API,
          # which avoids publishing the extra "sha256-<digest>" fallback tag.
          DIGEST=$(aws ecr-public describe-image-tags \
            --repository-name adot-autoinstrumentation-python \
            --region ${{ env.AWS_PUBLIC_ECR_REGION }} \
            --query "imageTagDetails[?imageTag=='v${{ env.VERSION }}'].imageDetail.imageDigest | [0]" \
            --output text)
          if [ -z "$DIGEST" ] || [ "$DIGEST" = "None" ]; then
            echo "Could not resolve a digest for v${{ env.VERSION }}; skipping image signing."
            exit 0
          fi
          notation sign ${{ env.RELEASE_PUBLIC_REPOSITORY }}@${DIGEST} \
            --force-referrers-tag=false \
            --plugin com.amazonaws.signer.notation.plugin \
            --id ${{ steps.ecr-signing-profile.outputs.profile_arn }}
