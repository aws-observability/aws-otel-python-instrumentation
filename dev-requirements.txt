pylint==3.0.2
flake8==6.1.0
isort==5.12.0
black==24.3.0
httpretty==1.1.4
mypy==0.931
pytest>=9
readme-renderer==42.0
bleach==4.1.0 # transient dependency for readme-renderer
protobuf>=5.29.5
markupsafe>=2.0.1
codespell==2.1.0
requests==2.32.5
ruamel.yaml==0.17.21
pyyaml==6.0.3
flaky==3.7.0
botocore~=1.0
# Needed so lint can resolve `import bytecode` in the DI bytecode-injection engine. It is a core
# runtime dependency (see pyproject.toml) installed on Python 3.10-3.11, but the lint env does not
# install the distro package itself, so it must be listed here too.
bytecode>=0.13.0
flask>=2.0.0
fastapi>=0.68.0
starlette>=0.14.2
django>=3.2
# Bumping this comment forces a tox cache rebuild so newly built ADOT wheels
# replace the previously cached install on PRs that change only src/.
